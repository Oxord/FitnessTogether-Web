#Configuration for WebApi
server {

    server_name fitness-together.ru;

    root /var/www/html;
    index index.html;

    # API requests go to backend
    location /api {
        proxy_pass http://localhost:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Frontend SPA - serve static files with fallback to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/fitness-together.ru/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/fitness-together.ru/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}

#Configuration for Kibana
server {
    server_name kibana.fitness-together.ru;

    allow 136.169.210.23;
    deny all;

    location / {
        proxy_pass http://localhost:5601;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/kibana.fitness-together.ru/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/kibana.fitness-together.ru/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}

server {
    if ($host = fitness-together.ru) {
        return 301 https://$host$request_uri;
    } # managed by Certbot

    server_name fitness-together.ru;
    listen 80;
    return 404; # managed by Certbot
}

server {
    if ($host = kibana.fitness-together.ru) {
        return 301 https://$host$request_uri;
    } # managed by Certbot

    listen 80;
    server_name kibana.fitness-together.ru;
    return 404; # managed by Certbot
}
